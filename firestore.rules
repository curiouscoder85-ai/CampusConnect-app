
/**
 * @file Firestore Security Rules
 * @description Optimized ruleset using denormalized fields for efficient ownership verification.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // Helper to check if the current user has a specific role.
    // Note: Use sparingly in list operations due to get() limits.
    function isRole(role) {
      return isSignedIn() && 
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == role;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }
    
    // Checks if the user is the teacher assigned to a specific course.
    function isTeacherOfCourse(courseId) {
        let course = get(/databases/$(database)/documents/courses/$(courseId));
        return isSignedIn() && course.data.teacherId == request.auth.uid;
    }

    match /users/{userId} {
      allow list: if isSignedIn();
      allow get: if isSignedIn();
      allow create: if (isOwner(userId) && request.resource.data.id == userId) || isRole('admin');
      allow update: if isOwner(userId) || isRole('admin');
      allow delete: if isRole('admin');

      match /chatHistory/{messageId} {
        allow list, get, create, update, delete: if isOwner(userId);
      }
    }

    match /courses/{courseId} {
      allow get: if isSignedIn();
      allow list: if isSignedIn();

      allow create: if isSignedIn() && 
                      request.resource.data.teacherId == request.auth.uid && 
                      isRole('teacher');
      
      allow update, delete: if isSignedIn() && (
        resource.data.teacherId == request.auth.uid || 
        isRole('admin')
      );
      
      match /submissions/{submissionId} {
        // Allow reading if the user is the teacher of the course, 
        // the student who submitted it, or an admin.
        allow get, list: if isSignedIn() && (
          resource.data.teacherId == request.auth.uid || 
          resource.data.userId == request.auth.uid ||
          isRole('admin') ||
          // Fallback for new documents during creation check
          (request.method == 'list' && isTeacherOfCourse(courseId))
        );
        
        allow create: if isSignedIn() && 
                        request.resource.data.userId == request.auth.uid && 
                        request.resource.data.courseId == courseId;
                        
        allow update: if isSignedIn() && (
          resource.data.teacherId == request.auth.uid || 
          isRole('admin')
        );
        
        allow delete: if isSignedIn() && (
          resource.data.teacherId == request.auth.uid || 
          resource.data.userId == request.auth.uid || 
          isRole('admin')
        );
      }
      
      match /feedback/{feedbackId} {
        allow get, list: if true; // Publicly readable for course insights
        allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;
        allow update, delete: if isSignedIn() && (
          resource.data.teacherId == request.auth.uid || 
          isRole('admin')
        );
      }
    }

    match /enrollments/{enrollmentId} {
      allow list: if isSignedIn();
      allow get: if isSignedIn() && (
        resource.data.userId == request.auth.uid || 
        resource.data.teacherId == request.auth.uid || 
        isRole('admin')
      );
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;
      allow update, delete: if isSignedIn() && (
        resource.data.userId == request.auth.uid || 
        resource.data.teacherId == request.auth.uid || 
        isRole('admin')
      );
    }

    match /users/{userId}/certificates/{certificateId} {
      allow get, list, create, update, delete: if isOwner(userId) || isRole('admin');
    }

    match /users/{userId}/recommendations/{recommendationId} {
      allow get, list, create, update, delete: if isOwner(userId) || isRole('admin');
    }
    
    // Collection group rules for optimized searching.
    // These rely on denormalized teacherId/userId for static rule verification.
    match /{path=**}/feedback/{feedbackId} {
       allow list: if isSignedIn() && (
         resource.data.teacherId == request.auth.uid || 
         resource.data.userId == request.auth.uid || 
         isRole('admin')
       );
    }

    match /{path=**}/submissions/{submissionId} {
       allow list: if isSignedIn() && (
         resource.data.teacherId == request.auth.uid || 
         resource.data.userId == request.auth.uid || 
         isRole('admin')
       );
    }
  }
}
